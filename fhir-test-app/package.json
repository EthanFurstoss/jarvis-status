{
  "name": "pcc-fhir-test-app",
  "version": "1.0.0",
  "description": "Test app that pulls test data from the PointClickCare FHIR API (with mock fallback)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
